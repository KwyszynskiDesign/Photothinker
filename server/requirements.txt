# RIP Preview Server Requirements
# Install with: pip install -r requirements.txt

PyMuPDF>=1.24.0
flask>=3.0.0
flask-cors>=4.0.0
Pillow>=10.0.0
numpy>=1.24.0

# ----------------------------------------------------------------------------
# WAZNE: Ghostscript (zewnetrzny program, NIE instalowany przez pip!)
# ----------------------------------------------------------------------------
# Dla PRAWDZIWYCH separacji (osobno C/M/Y/K + kazdy spot/Pantone/diecut
# dokladnie tak, jak zaznaczone w pliku) wymagany jest Ghostscript.
#
#   Windows:  https://ghostscript.com/releases/gsdnld.html
#             (zainstaluj, restart - aplikacja sama wykryje gswin64c)
#   macOS:    brew install ghostscript
#   Linux:    sudo apt install ghostscript   (lub: dnf/pacman)
#
# Bez Ghostscript serwer dziala dalej, ale separacje spot sa PRZYBLIZONE
# (fallback PyMuPDF). Status silnika widac w naglowku aplikacji.
